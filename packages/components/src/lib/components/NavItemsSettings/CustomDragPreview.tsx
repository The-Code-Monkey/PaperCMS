const CustomDragPreview = (props) => {
  const item = props.monitorProps.item;

  return (
    <div >
      <div>
        seomthing
      </div>
      <div>{item.text}</div>
    </div>
  );
};

export default CustomDragPreview;
